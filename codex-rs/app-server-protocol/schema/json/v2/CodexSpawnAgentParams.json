{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Fork a child thread from an existing parent thread and start its first turn with the supplied prompt.",
  "properties": {
    "agentRole": {
      "description": "Optional role recorded in the child thread's metadata.",
      "type": [
        "string",
        "null"
      ]
    },
    "model": {
      "description": "Optional model override for the child thread.",
      "type": [
        "string",
        "null"
      ]
    },
    "parentThreadId": {
      "description": "Parent thread to fork from.",
      "type": "string"
    },
    "prompt": {
      "description": "Initial task given to the child thread.",
      "type": "string"
    }
  },
  "required": [
    "parentThreadId",
    "prompt"
  ],
  "title": "CodexSpawnAgentParams",
  "type": "object"
}